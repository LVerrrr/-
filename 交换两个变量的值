#include <stdio.h>
int main(){
  	int x,y,t;
  	//交换两个变量的值 
  	scanf("%d %d",&x,&y);
  	t = x;   //copy 一份 
  	x = y;   //copy y 
  	y = t;   //把x的拷贝赋值给y 
  	printf("x = %d y = %d",x,y);
    return 0;
} 
